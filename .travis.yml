---

dist: focal
language: minimal

services:
  - docker

env:
  global:
    - DOCKER_REPO: 9jkh/certbot-dns

install:
  - make build
  - make test

deploy:
  provider: script
  script: make publish
  skip_cleanup: true
  on:
    branch: main
